# Copy to .env and fill in. Required for: docker compose up
# Single Postgres: one password for postgres superuser, propmanager user, and authentik user.

# --- Postgres (required; used for postgres, propmanager, and authentik DB users) ---
# Generate with: openssl rand -base64 36 | tr -d '\n'
POSTGRES_PASSWORD=

# --- Authentik (required) ---
# Generate with: openssl rand -base64 60 | tr -d '\n'
AUTHENTIK_SECRET_KEY=

# --- PropMange API (required after Authentik is set up) ---
# OIDC issuer URI from Authentik: create Provider → OIDC → use "Issuer" (internal URL so app can fetch JWKS).
# Example: http://authentik:9000/application/o/prop-manager/
AUTH_ISSUER_URI=

# --- Optional ---
# CORS_ALLOWED_ORIGIN=http://localhost
# AUTHENTIK_HTTP_PORT=9000
# AUTHENTIK_HTTPS_PORT=9443
# AUTHENTIK_IMAGE=ghcr.io/goauthentik/server
# AUTHENTIK_TAG=2025.4.4


# MAIL_HOST=sandbox.smtp.mailtrap.io
# MAIL_PORT=587
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_FROM=noreply@propmanager.com
# NOTIFICATION_EMAIL_ENABLED=true

# INVITE_EMAIL_RETRY_INTERVAL_MINUTES=1