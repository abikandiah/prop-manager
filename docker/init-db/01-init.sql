-- Initialization script for prop-manager database.
-- Add .sql or .sh files to docker/init-db/; they run in alphabetical order
-- on first container start (when the data volume is empty).

-- Example: enable extensions
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
